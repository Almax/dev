import React, {
	View
} from 'react-native';
import { connect } from 'react-redux';
class WebPage extends React.Component {
	render() {
		return (
			<View>

			</View>
		);
	}
}
export default connect()(WebPage);